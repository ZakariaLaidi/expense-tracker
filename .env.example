# Configuration de l'environnement pour Expense Tracker
# Copiez ce fichier en .env et remplissez les valeurs

# URL de connexion PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Pour Vercel, utilisez une base de données cloud (Neon, Supabase, Railway)
DATABASE_URL="postgresql://user:password@localhost:5432/expense_tracker?sslmode=require"

# Clé secrète pour la génération des tokens JWT
# Utilisez une chaîne aléatoire et sécurisée en production
JWT_SECRET="votre_cle_secrete_super_securisee_a_changer"

# Durée de validité du token JWT (ex: 1h, 7d, 30d)
JWT_EXPIRES_IN="7d"

# Port du serveur (optionnel, défaut: 3000)
PORT=3000

# Environnement (development, production)
NODE_ENV="development"

# URL du frontend (pour CORS en production)
FRONTEND_URL="https://votre-frontend.vercel.app"
